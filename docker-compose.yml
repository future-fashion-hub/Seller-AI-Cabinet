services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      - port=8080
    ports:
      - "8080:8080"

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    depends_on:
      - server
    ports:
      - "5173:80"
